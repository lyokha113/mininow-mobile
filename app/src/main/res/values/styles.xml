<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Ratingbar" style="?android:attr/ratingBarStyleIndicator">
        <item name="android:maxHeight">16dp</item>
    </style>

    <style name="MaterialDialogSheet" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@style/MaterialDialogSheetAnimation</item>
    </style>

    <style name="MaterialDialogSheetAnimation">
        <item name="android:windowEnterAnimation">@anim/show</item>
        <item name="android:windowExitAnimation">@anim/hide</item>
    </style>

    <style name="TabLayoutTextStyle">
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">normal</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="MaterialDialogSheetUp" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@style/MaterialDialogSheetAnimationUp</item>
    </style>

    <style name="MaterialDialogSheetAnimationUp">
        <item name="android:windowEnterAnimation">@anim/show_up</item>
        <item name="android:windowExitAnimation">@anim/hide_up</item>
    </style>

    <style name="MaterialDialogSheetDown" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@style/MaterialDialogSheetAnimationDown</item>
    </style>

    <style name="MaterialDialogSheetAnimationDown">
        <item name="android:windowEnterAnimation">@anim/show_down</item>
        <item name="android:windowExitAnimation">@anim/hide_down</item>
    </style>


</resources>
